# Terraform – Null Provider 

This repo contains the **simplest possible Terraform example** using the **null provider**,

## What it does
- Creates a `null_resource`
- Demonstrates how the null provider works with the least required config
- Does not run any commands or create any cloud resources